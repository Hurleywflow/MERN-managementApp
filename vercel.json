{
  "version": 2,
  "builds": [
    {
      "src": "NPM_CONFIG_PRODUCTION=false npm install --prefix client && npm run build --prefix client",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/"
    }
  ]
}
